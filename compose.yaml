services:
  web:
    build:
      context: .
    ports:
      - "127.0.0.1:5000:8000"
    volumes:
      - type: bind
        source: ./checkpoints_300.json
        target: /app/checkpoints_300.json
        read_only: true
    read_only: true
    tmpfs:
      - /tmp:size=64m,mode=1777
